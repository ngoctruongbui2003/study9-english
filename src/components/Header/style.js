import styled from '@emotion/styled'

export const WrapperAppbarText = styled.span({
    fontWeight: 'bold',
    cursor: 'pointer',
    color: '#71869d',
    '&:hover': {
        color: '#35509a'
    }
})